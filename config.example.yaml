# Optional non-secret settings. Copy to config.yaml if you want overrides.
# Secrets stay in .env (GROK_BRIDGE_TOKEN, webhook URL/auth).

server:
  host: "127.0.0.1"
  port: 18787

cli:
  base_url: "http://127.0.0.1:18787"
  poll_interval_sec: 1.5
  reply_timeout_sec: 90

queue:
  max_replies: 500
